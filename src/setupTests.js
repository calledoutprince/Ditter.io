// We removed @testing-library/jest-dom due to an unresolvable ESM Node.js conflict
// with its transitive dependencies (@csstools/css-calc).
// Tests now run purely using React DOM and standard JSDOM assertions.
